const asset = require('./asset');

const intl = require('./intl');

const server = require('./server');

const siteName = 'devlee.io';

module.exports = {
  asset: asset,
  intl: intl,
  server: server,
  siteName: siteName
};
